<% if user_signed_in? %>
  <%= custom_bootstrap_form_for(Subscription.new, as: :subscription, url: subscription_create_path, layout: :inline) do |f| %>
    <%= f.hidden_field item_type, value: true %>
    <%= f.hidden_field :email,
                       value: current_user.email %>

    <!-- if user_subscribed(item_type) -->
      <!-- Button for subscribed users
      <button class="btn btn-default pull-right" id="subscribed-button">
        <span class="subscribed-button-content"><i class='fa fa-check'></i> <%= t('subscriptions.subscribed') %></span>
        <span class="subscribed-button-content" style="display:none;"><i class='fa fa-times'></i> <%= t('subscriptions.sign_off') %></span>
      </button>
    -->
    <!-- else -->
      <button class="btn btn-primary pull-right" id="subscribe-button">
        <%= t('subscriptions.subscribe') %>
      </button>
    <!-- end -->

  <% end %>
<% else %>
  <button class="btn btn-primary pull-right subscription-toggle"><%= t 'subscriptions.subscribe' %></button>
<% end %>
