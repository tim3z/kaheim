<%= custom_bootstrap_form_for(Subscription.new, as: :subscription, url: subscriptions_create_path, layout: :inline) do |f| %>
  <%= t 'subscriptions.actions.subscribe' %>
  <%= f.hidden_field item_type, value: true %>
  <%= f.submit t('subscriptions.actions.subscribe'), class: 'btn btn-primary pull-right top-align',  id: 'subscription-submit-button', style: 'display: none' %>
  <span class="btn btn-default pull-right subscription-toggle top-align" id="subscription-back-button"><%= t('helpers.links.back') %></span>
  <%= f.email_field :email,
                    id: 'subscription-email',
                    placeholder: t('subscriptions.mail'),
                    class: 'top-align',
                    hide_label: true %>
<% end %>
