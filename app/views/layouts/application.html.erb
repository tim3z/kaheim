<!DOCTYPE html>
<html>
<head>
  <title>Wohnungsbörse</title>
  <!--[if lt IE 9]>
  <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
  <![endif]-->
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>

<div id="wrap">
  <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container-fluid">

      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#kaheim-navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to 'Kaheim', root_path, :class => 'navbar-brand' %>
      </div>


      <div class="collapse navbar-collapse" id="kaheim-navbar-collapse">

        <ul class="nav navbar-nav">
          <div class="btn-group">
            <button type="button" class="btn btn-default navbar-btn" data-link="<%= offers_path %>">
              <span class="badge badge-info"><%= (c = Offer.current.unlocked.count) > 0 ? c : '' %></span>
              <%= Offer.model_name.human count: c %>
            </button>
            <button type="button" class="btn btn-default navbar-btn dropdown-toggle" data-toggle="dropdown">
              <span class="caret"></span>
              <span class="sr-only">Toggle Dropdown</span>
            </button>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to t('offers.new.title'), new_offer_path %>
              </li>
            </ul>
          </div>
          <div class="btn-group">
            <button type="button" class="btn btn-default navbar-btn" data-link="<%= requests_path %>">
              <span class="badge badge-info"><%= (c = Request.current.unlocked.count) > 0 ? c : '' %></span>
              <%= Request.model_name.human count: c %>
            </button>
            <button type="button" class="btn btn-default navbar-btn dropdown-toggle" data-toggle="dropdown">
              <span class="caret"></span>
              <span class="sr-only">Toggle Dropdown</span>
            </button>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to t('requests.new.title'), new_request_path %>
              </li>
            </ul>
          </div>
        </ul>

        <ul class="nav navbar-nav navbar-right">
          <li class="language">
            <%= link_to params.merge(locale: :de) do %>
              <%= image_tag 'de.png' %>
            <% end %>
          </li>
          <li class="language">
            <%= link_to params.merge(locale: :en) do %>
              <%= image_tag 'en.png' %>
            <% end %>
          </li>

          <% if user_signed_in? %>
              <li>
                <%= link_to t("helpers.signed_in_message", :mail => current_user.email), edit_user_registration_path %>
              </li>
              <% if current_user.admin %>
                  <li><%= link_to 'Admin', '/admin' %></li>
              <% end %>
              <li>
                <%= link_to t("helpers.links.sign_out"), destroy_user_session_path, :method => :delete %>
              </li>
          <% else %>
              <li>
                <%= link_to t("helpers.links.sign_up"), new_user_registration_path %>
              </li>
              <li class="dropdown">
                <a class="dropdown-toggle" href="#" data-toggle="dropdown"><%= t("helpers.links.sign_in") %><strong class="caret"></strong></a>
                <div class="dropdown-menu" style="padding: 15px; width: 300px;">
                  <%= render :partial => "devise/sessions/new" %>
                </div>
              </li>
          <% end %>
        </ul>
      </div>

    </div>
  </nav>

  <div class="container-fluid clear-top" id="main">
    <div class="col-md-2"></div>
    <div class="col-md-8">
      <!--<div class="row">-->
        <% { error: 'error', alert: 'danger', notice: 'success', info: 'info' }.each do |k, v| %>
          <% if flash[k] %>
            <div class="alert alert-<%= v %> alert-dismissible" role="alert">
              <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
              <div id="flash_<%= k %>">
                <%= flash[k] %>
              </div>
            </div>
          <% end %>
        <% end %>
      <!--</div>-->
      <%= yield %>
    </div>
    <div class="col-md-2"></div>
  </div>
</div>

<footer class="footer">
  <div class="container">
    <p>Problems, feedback and feature requests on <%= link_to 'GitHub', 'https://github.com/SDEagle/je-ka-wg/issues?state=open' %> or by <%= link_to 'E-Mail', 'mailto:dev.tim.zeitz@gmail.com' %></p>
    <p>Source availalbe at <%= link_to 'GitHub', 'https://github.com/SDEagle/je-ka-wg' %> - feel free to contribute</p>
    <p>Implementation by <%= link_to 'Tim Zeitz', 'https://github.com/SDEagle' %></p>
    <p>Impressum: </p>
  </div>
</footer>

</body>
</html>
