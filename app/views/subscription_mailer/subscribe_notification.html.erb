Hallo,

Dein Abonnement für neue <%=
    if @subscription.offers && @subscription.requests
      'Angebote und Gesuche'
    elsif @subscription.offers
      'Angebote'
    else
      'Gesuche'
    end %> auf Kaheim ist jetzt aktiv.

Um Dich davon wieder abzumelden, klicke auf diesen
<%= link_to 'Link', subscription_unsubscribe_url( @subscription.subscribed_types, @subscription.unsubscribe_token, :protocol => "https") %>.