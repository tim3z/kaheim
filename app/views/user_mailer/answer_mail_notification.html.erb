<p>Hallo,</p>

<p>du hast gerade über Kaheim eine Nachricht an "<%= @item_username %>" zur Anzeige "<%= @item.title %>" gesendet:</p>

<%= simple_format @content %>

<%= link_to t('helpers.links.to_item'), @item %>
